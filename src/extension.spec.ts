import * as assert from 'assert';

/**
 * These tests are unit tests.
 * Invoked code cannot access the VS Code API.
 * 
 * If you need to test the extension behavior, use the end-to-end tests in `extension.test.ts`.
 */

suite('Extension Test', () => {

    test('Sample test', () => {
        // Arrange

        // Act

        // Assert
        assert.ok(true);
    });
});
