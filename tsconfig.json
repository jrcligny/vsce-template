{
	/**
	 * This configuration is only used to check types or build tests.
	 * 
	 * Output files are generated by the build task 'bundleApp'
	 * @see .tasks/build/bundleApp.js
	 */
	// current node version of vscode is 20.18
	"extends": "@tsconfig/node20/tsconfig.json",
	"compilerOptions": {
		"outDir": "out-test",
		"sourceMap": true
	},
	"include": [
		"test",
		"src",
	],
}